def add_arguments(parser):
    '''
    Add your arguments here if needed. The TAs will run test.py to load
    your default arguments.
	

    For example:
        parser.add_argument('--batch_size', type=int, default=32, help='batch size for training')
        parser.add_argument('--learning_rate', type=float, default=0.01, help='learning rate for training')
    '''
    # parser.add_argument('gamma', type=float, default=32, help='discount factor')
    # parser.add_argument('epsilon', type=float, default=32, help='exploration factor')
    # parser.add_argument('alpha', type=float, default=32, help='learning_rate')
    # parser.add_argument('maxMermorySize', type=float, default=32, help='--')
    # parser.add_argument('epsEnd', type=float, default=32, help='min exploration factor')
    # parser.add_argument('replace', type=int, default=32, help='when to replace')
    # parser.add_argument('actionSpace', type=float, default=32, help='discount factor')

    return parser
